#include <iostream>

using namespace std;

int main(void)
{
    int gross_pay;
    cout<<"Enter your gross pay :"<<" ";
    cin>>gross_pay;
    return (0);
}
double tax_Pay(int money)
{
    int tax;
    int rate;
    if (money > 40,000)
    {
        rate = 0.3;
        
    }
    

    return tax;
}